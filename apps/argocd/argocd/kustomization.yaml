---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - namespace.yaml
  - github.com/argoproj/argo-cd/manifests/cluster-install?ref=v3.1.9
  - bootstrap-app-set.yaml
  - argocd-external-secret.yaml
patches:
  - path: config.yaml
  - path: config-cmd-params.yaml
  - path: service.yaml
# Note: rbac-cm.yaml needs to be applied manually until we fix kustomize patching
# kubectl apply -f apps/argocd/argocd/rbac-cm.yaml
